# API backend URL (server-side only, used by the Next.js rewrite proxy)
# In development, this points to the local wrangler dev server.
# In production, next.config.ts falls back to the deployed Worker URL automatically.
API_BACKEND_URL=http://localhost:8787