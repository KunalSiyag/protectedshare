# API backend URL
# In development, leave this EMPTY to use Next.js rewrite proxy (/api/* -> localhost:8787).
# In production, set to your deployed Worker URL (e.g. https://api.protectedshare.me).
NEXT_PUBLIC_API_BASE_URL=